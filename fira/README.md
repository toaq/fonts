# Fira Sans

This is a work in progress.

`add_toaq.py` is a FontForge Python script that extracts the original font files from `original.dat`, then adds ꝡ and Deranı glyphs to them, and finally just generates some PDF samples for now.

