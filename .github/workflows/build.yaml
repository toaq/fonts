name: Build
on:
  push:
    branches: [main]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nixbuild/nix-quick-install-action@v19
        with:
          nix_conf: experimental-features = nix-command flakes
      - name: Build derani-fonts
        run: nix build .#derani-fonts -o derani-fonts
      - name: Build latin-fonts
        run: nix build .#latin-fonts -o latin-fonts
      - uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: '${{ secrets.GITHUB_TOKEN }}'
          automatic_release_tag: latest
          prerelease: false
          files: '{derani,latin}-fonts/share/fonts/*/*'
